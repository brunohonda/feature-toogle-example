// import { TestBed } from '@angular/core/testing';
// import { CanActivateFn } from '@angular/router';

// import { featureToogleGuard } from './feature-toogle-guard';

// describe(featureToogleGuard.name, () => {
//   const executeGuard: CanActivateFn = (FeatureToggleEnum) =>
//       TestBed.runInInjectionContext(() => featureToogleGuard(...guardParameters));

//   beforeEach(() => {
//     TestBed.configureTestingModule({});
//   });

//   it('should be created', () => {
//     expect(executeGuard).toBeTruthy();
//   });
// });
